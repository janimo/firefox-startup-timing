A script that runs Firefox a few times with a modified version of the AboutStartup addon,
to collect startup timings in cold and warm caches without user interactivity.
This is meant to be run automatically to collect data over time.
Tested with Firefox 4 on Ubuntu 11.10 development version.
